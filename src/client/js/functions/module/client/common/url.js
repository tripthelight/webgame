const W_PROTOCOL = window.location.protocol;
const W_HOST = window.location.host;
const W_HOSTNAME = window.location.hostname;
const W_PATHNAME = window.location.pathname;

export { W_PROTOCOL, W_HOST, W_HOSTNAME, W_PATHNAME };
